# SDF_Slicer
Quick slicer, thrown together for use with the stratasys polyjet line &amp; basic, analytically derived implicit bodies.

Note to self: Load the conda env using 
`conda env create -f environment.yml`
and save it via
`conda env export > environment.yml`